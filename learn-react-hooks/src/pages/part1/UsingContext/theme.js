export const theme = (isDarkMode) => ({
  backgroundColor: isDarkMode ? 'black' : 'white',
  color: isDarkMode ? 'white' : 'black',
});

export const mainTheme = (isDarkMode) => ({
  backgroundColor: isDarkMode ? 'gray' : 'white',
  color: isDarkMode ? 'white' : 'black',
});
